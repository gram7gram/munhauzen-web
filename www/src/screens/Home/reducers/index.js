import {combineReducers} from 'redux';

const isLoading = (prev = false, action) => {
  switch (action.type) {
    default:
      return prev
  }
}


export default combineReducers({
  isLoading,
});
